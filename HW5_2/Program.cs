using System;
using HW5_2;

namespace MyApp // Note: actual namespace depends on the project name.
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
            CommandExecuter commandExecuter = new CommandExecuter();
            ICommand command = new Command();

           //  Добавляем 1000 команд в очередь
            for (int i = 0; i < 1000; i++)
            {
                commandExecuter.AddCommand(command);
            }
            // Запускаем обработку сообщений в 2 потока
            commandExecuter.StartExecution();
            Thread.Sleep(500);
            // Запускаем команду мягкой остановки
            commandExecuter.SetSoftStop();
            Console.WriteLine("GG:" + commandExecuter.CountCommandsInQueue());
            commandExecuter.AddCommand(command);
            // Ждем 1000 мс для убеждения, что очередь не обрабатыватся
            Console.WriteLine("G1:" +commandExecuter.CountCommandsInQueue());
            Console.WriteLine("G2:" + commandExecuter.CountCommandsInQueue());
            Console.WriteLine("G3:" + commandExecuter.CountCommandsInQueue());
            
            */

        }
    }
}